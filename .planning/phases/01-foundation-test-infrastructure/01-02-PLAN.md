---
phase: 01-foundation-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/__init__.py
  - tests/test_tools.py
  - tests/test_sandbox.py
  - tests/test_agent_core.py
autonomous: true
requirements:
  - FOUND-02
must_haves:
  truths:
    - "Running pytest in the project root discovers and passes all tests with zero failures"
    - "Filesystem security sandbox boundary is tested against path traversal attacks"
    - "Core tools (read_file, write_file, replace_edit, insert_content) have complete unit test coverage"
    - "Conversation pruning logic in agent_core is verified"
  artifacts:
    - tests/__init__.py
    - tests/test_tools.py
    - tests/test_sandbox.py
    - tests/test_agent_core.py
  key_links:
    - "tests/test_tools.py tests Agent.tools functions against an isolated tmp_path"
    - "tests/test_sandbox.py tests Agent.sandbox execution and timeout handling"
    - "tests/test_agent_core.py tests Agent.agent_core.prune_messages and turn grouping"
---

<objective>
Implement an automated unit and integration test suite with `pytest` in `tests/` to establish comprehensive regression testing for Codey's core tools and agent logic.

Purpose: Fills the testing void (where `pytest` currently exits with error due to 0 collected tests) and validates that filesystem containment, subprocess sandboxing, and message pruning operate reliably.
Output: Test suite files in `tests/` passing under `pytest`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-test-infrastructure/01-01-PLAN.md
@Agent/tools.py
@Agent/sandbox.py
@Agent/agent_core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement filesystem and tool unit tests</name>
  <files>tests/test_tools.py, tests/__init__.py</files>
  <read_first>Agent/tools.py</read_first>
  <action>
Create `tests/__init__.py` and `tests/test_tools.py`:
- Test `_safe_path`:
  - Valid relative path inside workspace resolves correctly.
  - Path traversal attempt (e.g., `../../etc/passwd` or `..\..\windows\win.ini`) raises `ValueError`.
- Test `write_file` and `read_file`:
  - Creating new file, writing content with nested parent directories, reading content back accurately.
  - Reading non-existent file raises `FileNotFoundError`.
- Test `replace_edit`:
  - Successfully replaces exact unique matching string.
  - Raises `ValueError` when `old_str` is not found (0 matches).
  - Raises `ValueError` when `old_str` matches multiple times (>1 matches).
- Test `insert_content`:
  - Inserts content `before` target anchor.
  - Inserts content `after` target anchor.
  - Appends to end of file when target is omitted with position='after'.
  - Prepends to start of file when target is omitted with position='before'.
- Test `list_dir`:
  - Lists files inside directory correctly, omitting `.git` directories.
Use monkeypatch or temporary workspace redirect to ensure tests do not pollute the real workspace directory.
  </action>
  <verify>pytest tests/test_tools.py -v</verify>
  <acceptance_criteria>
    - `tests/test_tools.py` passes all test cases under `pytest`.
  </acceptance_criteria>
  <done>Filesystem tools have verified unit tests covering happy paths, edge cases, and security boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sandbox execution and timeout unit tests</name>
  <files>tests/test_sandbox.py</files>
  <read_first>Agent/sandbox.py</read_first>
  <action>
Create `tests/test_sandbox.py`:
- Test `run_sandbox` executes a basic command (e.g. `python -c "print('hello')"`).
- Test return dictionary structure: `stdout`, `stderr`, `exit_code`.
- Test non-zero exit code capture (e.g. `python -c "import sys; sys.exit(42)"`).
- Test timeout handling: command that exceeds timeout is killed and returns `exit_code: -1` with descriptive timeout message in `stderr`.
  </action>
  <verify>pytest tests/test_sandbox.py -v</verify>
  <acceptance_criteria>
    - `tests/test_sandbox.py` passes all test cases under `pytest`.
  </acceptance_criteria>
  <done>Sandbox process execution and timeout behavior verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement agent core conversation pruning and turn tests</name>
  <files>tests/test_agent_core.py</files>
  <read_first>Agent/agent_core.py</read_first>
  <action>
Create `tests/test_agent_core.py`:
- Test `_group_into_turns`:
  - Correctly groups user, assistant, and tool messages into atomic turns.
- Test `prune_messages`:
  - Preserves the first two anchor messages (system prompt + user task).
  - Keeps message count within `max_messages` while maintaining turn integrity.
  - Does not prune when message count is <= `max_messages`.
- Test tool dispatch error handling:
  - Unknown tool name produces structured `ERROR: Unknown tool '...'` string rather than raising an unhandled exception.
  - Tool execution failure produces structured `ERROR: ...` string.
  </action>
  <verify>pytest tests/test_agent_core.py -v</verify>
  <acceptance_criteria>
    - `tests/test_agent_core.py` passes all test cases under `pytest`.
  </acceptance_criteria>
  <done>Agent core turn management, history pruning, and error resilience tested.</done>
</task>

<task type="auto">
  <name>Task 4: Run full test suite regression</name>
  <files>tests/</files>
  <read_first>pyproject.toml</read_first>
  <action>
Execute `pytest` across the entire `tests/` directory to verify full test discovery and 100% passing status.
  </action>
  <verify>pytest -v</verify>
  <acceptance_criteria>
    - `pytest` exits with code 0.
    - Zero failed tests.
  </acceptance_criteria>
  <done>Full test suite passing.</done>
</task>

</tasks>
