---
phase: 01-foundation-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Agent/__init__.py
  - Agent/cli.py
  - Agent/agent_core.py
  - Agent/tools.py
  - Agent/planner.py
  - Agent/verifier.py
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "Running python -c \"import Agent.cli\" succeeds without ModuleNotFoundError"
    - "Internal imports resolve consistently whether running as a package or directly"
    - "The codey entry point in pyproject.toml resolves Agent.cli:main without import errors"
  artifacts:
    - Agent/__init__.py
    - Agent/cli.py
    - Agent/agent_core.py
    - Agent/tools.py
    - Agent/planner.py
    - Agent/verifier.py
  key_links:
    - "Agent/cli.py imports agent_core, audit, planner, verifier, ui from Agent package"
    - "Agent/agent_core.py imports tools, ui from Agent package"
    - "Agent/tools.py imports sandbox from Agent package"
---

<objective>
Fix internal package import resolution in `Agent/` and establish `Agent/__init__.py` so Codey can be imported and executed as a standard Python package.

Purpose: Resolves critical tech debt where un-prefixed imports crash when Codey is run via `python -m Agent.cli` or through the console script entry point `codey = "Agent.cli:main"`.
Output: `Agent/__init__.py` and refactored import statements across all modules in `Agent/`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
@Agent/cli.py
@Agent/agent_core.py
@Agent/tools.py
@Agent/planner.py
@Agent/verifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Agent/__init__.py and define package exports</name>
  <files>Agent/__init__.py</files>
  <read_first>pyproject.toml, Agent/cli.py</read_first>
  <action>Create `Agent/__init__.py` marking `Agent` as an explicit Python package and exporting `__version__ = "0.1.0"`.</action>
  <verify>python -c "import Agent; print(Agent.__file__)"</verify>
  <acceptance_criteria>
    - `Agent/__init__.py` exists on disk.
    - `import Agent` succeeds without error.
  </acceptance_criteria>
  <done>`Agent/__init__.py` is created and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor internal imports across Agent modules to package-qualified or relative imports</name>
  <files>Agent/cli.py, Agent/agent_core.py, Agent/tools.py, Agent/planner.py, Agent/verifier.py</files>
  <read_first>Agent/cli.py, Agent/agent_core.py, Agent/tools.py, Agent/planner.py, Agent/verifier.py</read_first>
  <action>
Update import statements across `Agent/` modules:
- In `Agent/cli.py`: change `from agent_core import ...` to `from Agent.agent_core import ...` (or relative `from .agent_core import ...` with fallback/dual support `try: from .xxx import ... except ImportError: from Agent.xxx import ...`), same for `audit`, `planner`, `verifier`, `ui`.
- In `Agent/agent_core.py`: change `from tools import ...` and `import ui` to package-qualified or relative imports.
- In `Agent/tools.py`: change `from sandbox import run_sandbox` to package-qualified or relative import.
- In `Agent/planner.py`: change `from agent_core import get_client` and `import ui` to package-qualified or relative import.
- In `Agent/verifier.py`: change `from agent_core import ...` and `from sandbox import run_sandbox` to package-qualified or relative import.
Ensure compatibility when executed both via `python -m Agent.cli` and directly `python Agent/cli.py`.
  </action>
  <verify>python -c "import Agent.cli; print('Agent.cli imported successfully')"</verify>
  <acceptance_criteria>
    - `python -c "import Agent.cli"` exits with code 0.
    - `python -c "import Agent.agent_core"` exits with code 0.
    - `python -c "import Agent.tools"` exits with code 0.
    - `python -c "import Agent.verifier"` exits with code 0.
  </acceptance_criteria>
  <done>All modules in `Agent/` resolve imports cleanly under package execution.</done>
</task>

<task type="auto">
  <name>Task 3: Verify CLI entry point execution</name>
  <files>pyproject.toml, Agent/cli.py</files>
  <read_first>pyproject.toml</read_first>
  <action>
Verify that `Agent.cli:main` can be imported and the banner/help can be invoked without runtime import errors.
Test with `python -c "from Agent.cli import main; print(callable(main))"`.
  </action>
  <verify>python -c "from Agent.cli import main; assert callable(main); print('CLI main is verified')"</verify>
  <acceptance_criteria>
    - `from Agent.cli import main` executes and confirms `main` is callable.
  </acceptance_criteria>
  <done>CLI entry point verified and ready for end-user execution.</done>
</task>

</tasks>
